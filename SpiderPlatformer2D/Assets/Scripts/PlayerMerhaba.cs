using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerMerhaba : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Merhaba pipi");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
